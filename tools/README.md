# Tools

`rmvx_embedded_export_tool_page483.rb` is a byte-preserved source reference for the protected in-game export tool. It is not modified by this migration.

The Git bootstrap package also contains `authority/SCRIPT_INDEX.json` / `.tsv`, which are the migration-time index for diff/rollback/blame workflows.
